"use strict";
const authCatalog={ja:{heading:"自分の記録へ、戻る。",lead:"秘密値はURL、analytics、browser storageへ残しません。",login:"ログイン",register:"アカウント登録",user:"ユーザーID",display:"表示名",password:"パスワード",timezone:"タイムゾーン",terms:"利用規約に同意する",privacy:"プライバシー規約に同意する",recover:"復旧コードでログイン",code:"復旧コード",reset:"再設定メールを送る",email:"メール",codesTitle:"復旧コードを今保存してください",codesHelp:"再表示できません。安全なpassword managerへ保存してください。",saved:"保存しました"},ko:{heading:"내 기록으로 돌아가기.",lead:"비밀값은 URL, analytics, browser storage에 남기지 않습니다.",login:"로그인",register:"계정 만들기",user:"사용자 ID",display:"표시 이름",password:"비밀번호",timezone:"시간대",terms:"이용약관에 동의",privacy:"개인정보 규정에 동의",recover:"복구 코드로 로그인",code:"복구 코드",reset:"재설정 메일 보내기",email:"이메일",codesTitle:"복구 코드를 지금 저장하세요",codesHelp:"다시 표시할 수 없습니다. 안전한 password manager에 저장하세요.",saved:"저장했습니다"}};
let locale=localStorage.getItem("oraja_locale")==="ko"?"ko":"ja";const status=document.getElementById("auth-status");
function setLocale(value){locale=value;localStorage.setItem("oraja_locale",value);document.documentElement.lang=value;for(const node of document.querySelectorAll("[data-auth]"))node.textContent=authCatalog[value][node.dataset.auth];}
function body(form){const data=Object.fromEntries(new FormData(form));for(const key of ["terms_consent","privacy_consent"])if(key in data)data[key]=true;return data;}
async function submit(path,value){const response=await fetch(path,{method:"POST",credentials:"same-origin",headers:{"content-type":"application/json"},body:JSON.stringify(value)});const payload=await response.json().catch(()=>({}));if(!response.ok)throw new Error(payload.error?.code||`HTTP ${response.status}`);return payload;}
async function handle(form,path,onSuccess){form.addEventListener("submit",async event=>{event.preventDefault();status.textContent="…";try{const result=await submit(path,body(form));status.textContent="";await onSuccess(result);}catch(error){status.textContent=error.message;status.style.color="#b13b30";}});}
handle(document.getElementById("login-form"),"/v1/auth/login",async()=>location.replace("/"));
handle(document.getElementById("recovery-form"),"/v1/auth/recovery",async()=>location.replace("/"));
handle(document.getElementById("reset-form"),"/v1/auth/password-reset/request",async()=>{status.textContent=locale==="ja"?"該当する場合はメールを送信しました。":"해당하는 경우 메일을 보냈습니다.";});
handle(document.getElementById("register-form"),"/v1/auth/register",async result=>{document.getElementById("recovery-codes").textContent=(result.recovery_codes||[]).join("\n");document.getElementById("recovery-dialog").showModal();});
document.getElementById("recovery-saved").onclick=()=>{document.getElementById("recovery-codes").textContent="";document.getElementById("recovery-dialog").close();location.reload();};document.getElementById("auth-locale").onclick=()=>setLocale(locale==="ja"?"ko":"ja");setLocale(locale);
